# node構成

わかりにくいので、on/offをするときはトグルを使わない。代わりに、ONのチェックボックスとOFFのチェックボックスそれぞれが
表示されていて、どちらか片方にだけ必ずチェックが入っているという形式にする。

main
|- typing
	|- text(label)
	|- kana(label)
	|- input(label/input)
	|- convert(on/off button)
	|- timer
		|- start/stop(button, shortcut)
		|- time(pulldownbox)
			|- custom_time(spinbox)
	|- keyboard layout select(pulldownbox?)
|- record
	|- convert on/off
	|- keyboard layout
	|- prompt set
	|- start year month day hour minute second
	|- end year month day hour minute second
	|- score
	|- graph
	|- miss
	|- slowing / accelerating
	|- until next type time
	|- jsonl format
|- settings
	|- back(button)
	|- fontsize
		|- now_fontsize(label)
		|- new_fontsize(spinbox)
	|- linebreak
		|- now_linebreak(label)
		|- new_linebreak(spinbox)
	|- kana visible on/off
	|- convert on/off visible on/off
	|- timer visible on/off
	|- keyboard layout select on/off
	
|- exit(button)
