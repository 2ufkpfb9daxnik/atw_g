# node構成

main
|- exit
|- typing
	|- text(label)
	|- kana(label)
	|- input(label/input)
	|- timer
		|- time
	|- 
|- settings
	|- back
	|- fontsize
		|- now_fontsize(label)
		|- new_fontsize(spinbox)
		|- confirm(button)
	|- linebreak
		|- now_threshold(label)
		|- new_threshold(spinbox)
		|- confirm(button)
